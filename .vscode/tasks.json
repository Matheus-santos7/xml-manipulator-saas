{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Build project",
			"type": "shell",
			"command": "command -v pnpm >/dev/null 2>&1 && pnpm install && pnpm run build || (npm install && npm run build)",
			"group": "build"
		}
	]
}